<% count = local_assigns.fetch(:count) { Rails.cache.fetch("landing/signup_count", raw: true, expires_in: 30.seconds) { User.count + Rsvp.count }.to_i } %>
<span id="rsvp_counter" class="rsvp-counter" data-count="<%= count %>"><%= number_with_delimiter(count) %></span>
